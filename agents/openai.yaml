interface:
  display_name: "PPTX Generator"
  short_description: "Create, edit, and visually verify PowerPoint decks"
  default_prompt: "Use $pptx-generator to create an editable, source-tracked PowerPoint deck and verify every rendered slide."
